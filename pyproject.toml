[project]
name = "forge"
version = "0.1.0"
description = "Forge project management workspace tooling"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "graphifyy",
]

[tool.uv]
package = false
